# 导航参考实现检查

final result: passed

## 检查范围与证据

- 来源：[用户提供的导航参考图](docs/references/sidebar-reference.png)，2400 × 1802 像素。
- 实现：[桌面截图](docs/references/sidebar-desktop.png)，1440 × 1000 CSS 像素，deviceScaleFactor 1。
- 同一比较输入：[导航并排对照](docs/references/sidebar-comparison.png)，650 × 1040 像素。
- 手机状态：[展开](docs/references/sidebar-mobile-expanded.png)、[收起](docs/references/sidebar-mobile-collapsed.png)，390 × 844 CSS 像素，deviceScaleFactor 1；另外检查了 320 × 700。
- 渲染使用本次任务单独启动的 Chrome Headless Shell。当前工具没有可操作的应用内浏览器，因此使用独立临时浏览器配置，不读取用户浏览器资料。

参考图是带外部说明的设计图。将其中侧栏裁切并等比缩至 280px 宽，与实现的 280px 侧栏放在同一张图片中检查。源图导航区域约为 x=611、y=133、w=488、h=1536；缩放约 0.574。实现栏高随视口变化，本次为 952px。它们不是同一内容量的业务页面，比较对象是导航组件风格。

全视图用于检查圆角、侧栏比例和留白；并排图本身已是聚焦侧栏的局部证据，顶部品牌区域、第一条分组、箭头和子级连接线以 1 倍 CSS 尺寸清晰可读，无需额外放大。

## 明确保留的范围差异

- 用户要求基础骨架，因此只接入现有“工作台 → 首页”，没有复制图片中的业务菜单和数字 112。
- 品牌文本为项目名称 Dashboard，图标为 Tabler 的布局图标；未仿造 Brainwave 标识。
- 当前分组含选中页面，因此参考其浅灰选中态；子项使用深色文字。
- 右侧注释、虚线框和外部指示线不属于导航，未加入产品界面。
- 主内容区保持原有骨架，未按参考图设计新的业务页面。

## 五项视觉检查

| 检查面     | 结果                                                                                                                                  |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 导航使用 Helvetica Neue / 苹方 / 微软雅黑；品牌 24px，菜单 15px。分组和当前子项字重突出；中文替换与源图英文宽度不同属于明确范围差异。 |
| 间距与布局 | 280px 栏宽、28px 圆角、52px 行高；调整横向间距后，父子文本起点对齐，连接线与图标中心对应。移动端多余空白已修复。                      |
| 颜色       | 白色面板、#191919 正文、#808080 图标、#f0f0f0 选中底与浅灰层级线，符合参考图的黑白灰关系。                                            |
| 图标与素材 | 使用 Tabler 官方图标组件，无模糊截图图标、手绘 SVG 或 CSS 品牌仿制。弧形层级线是列表连接边框。                                        |
| 文案与内容 | 仅显示现有真实路由；没有虚构数量、无法操作的业务入口或参考图中的说明文字。                                                            |

## 比较与修正历史

1. 初次并排和移动端截图检查：发现手机上的自动网格行被视口剩余高度撑开，导航与主内容之间出现大块空白（P2）。同时父子文本横向起点有偏差，分组字重偏轻（P2）。
2. 修正：承载布局设置 `align-content: start`；明确分组按钮字重，统一父子文字起点，调整面板内边距、图标与标题间距。
3. 重新捕获本文件列出的桌面、手机与并排截图后检查：多余空白消失，导航层级对齐，无未解决的 P0 / P1 / P2 问题。

## 交互检查

- 桌面分组点击收起、Enter / Space 展开。
- 子项按 Escape 收起分组并回到分组按钮。
- 当前路由有 aria-current；404 状态不错误选中首页，返回首页正常。
- 手机菜单展开、收起，点击链接后关闭并恢复按钮焦点，Escape 关闭。
- 390px 和 320px 宽度没有横向溢出。
- 浏览器运行时异常与错误日志：0。

## 检查边界

当前验证覆盖实际接入的一组菜单和首页。配置支持更多分组、嵌套项与可选数量，但尚无对应业务数据；后续接入实际菜单时应验证长标题、较深层级与更多内容的滚动表现。未做跨浏览器视觉回归。

## 完成项

- 已修正所有本次发现的 P2 问题。
- 已检查字体、布局、颜色、素材、文案与核心导航交互。
- 已保留图片、组件、菜单配置和全局文档。
