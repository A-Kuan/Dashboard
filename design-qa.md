# 报价模板视觉还原检查

final result: passed

## 对照证据

- 视觉来源：[用户选定的 16:9 效果图](docs/references/quote-template-reference.png)，1672 × 941 像素。
- 导航来源：[用户提供的侧栏参考图](docs/references/sidebar-reference.png)，2400 × 1802 像素；图右侧注释和引导线不属于界面。
- 浏览器实现：[最终截图](docs/references/quote-template-implementation.png)，1672 × 941 CSS 像素，deviceScaleFactor 1。
- 同尺寸并排输入：[视觉对照图](docs/references/quote-template-comparison.png)，3344 × 941 像素；左为来源，右为实现。
- 导航局部对照：[侧栏并排图](docs/references/sidebar-current-comparison.png)，512 × 941 像素；左侧为参考图导航裁切并等比缩至 256px，右侧为实现侧栏原尺寸裁切。
- 路由与状态：`/quotes/templates`，默认选中“卡宴滤芯组合”，无弹窗。浏览器为 Codex 应用内浏览器。

报价来源和实现均为完整页面，无浏览器边框；不需要缩放或裁切。报价并排图可判断三栏比例、标题、表格、按钮及内容密度。导航局部图用于核对品牌区域、分组按钮、弧形子项线、顶层链接、分隔线和浅灰选中态。参考图的外边距与圆角已被用户后续的“四周不要留白”要求覆盖；模块名称和菜单数量按当前真实路由呈现。未发现仍需修复的 P0 / P1 / P2 差异。

用户后续指出模板栏顶部的“报价模板”标题不需要。当前实现已移除该标题，搜索框与新建按钮上移；因此并排图中的这一处差异是最新明确要求。

## 五项视觉检查

| 检查面     | 结果                                                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------------------ |
| 字体与层级 | 使用 Helvetica Neue / 苹方 / 微软雅黑回退；品牌、分栏标题、车型标题和行文字的字重层级与效果图接近。                      |
| 间距与布局 | 256px 导航栏、437px 模板栏与剩余详情区对齐最终选定的视觉图；模板栏贴齐上下边缘，分组与底部操作区都在 1672 × 941 视口内。 |
| 颜色       | 白色基底、浅灰分隔、浅蓝选中项与提示条、蓝色主按钮及操作文字符合来源配色。                                               |
| 素材与图标 | 来源没有照片或插画；所有界面图标使用已有 Tabler Icons。未把整张效果图当页面背景。                                        |
| 文案与内容 | 默认展示卡宴 E3、混动卡宴、卡宴 92A 的五项马勒滤芯需求，模板列表和提示文案与来源一致；未虚构零件号、库存或价格。         |

## 比较与修正历史

1. 初次在 1672 × 941 视口对照时，实现的提示条和配件区整体偏上，左侧模板项与详情表格过密（P2）。调整标题区、模板项和表格行高，再次捕获截图后与来源对齐。
2. 并排检查发现实现缺少来源中各配件行左侧的勾选框（P2）。加入可操作的勾选框及“删除已选”，重新捕获最终截图。勾选框、行位置和底部按钮与来源接近。
3. 用户明确要求报价模板成为独立模块，移动到 `/quotes/templates` 后，初版侧栏新增“报价管理”分区标题，与导航参考图的顶层菜单样式不符（P2）。改为顶层“报价模板”链接，加入浅灰虚线分隔与选中态，并与原始侧栏参考图做局部并排复查。
4. 最终对照没有未解决的 P0 / P1 / P2 问题。图像生成本身有少量非标准字形和细节，代码使用标准中文与可操作控件处理，不逐像素复刻这些生成噪声。
5. 用户改选 `exec-6a2bd341...png` 版本后，模板栏改为 437px 直角贴边布局；搜索与深色新建按钮同行，加入“常用模板”“其他模板”和底部管理栏。重新生成 1672 × 941 同尺寸并排图，分栏边界和各模板行高度与该版本接近。

## 交互与运行检查

- 在浏览器中完成新建模板、修改名称、添加“火花塞”配件、打开报价草稿、填写价格和复制报价文字；剪贴板内容与表单值一致。
- 模板搜索、切换、编辑、增删条目和勾选批量删除均有对应界面状态。测试操作后已刷新，恢复默认示例状态。
- 管理模板弹窗可打开并删除模板；删除后总数从 7 更新为 6，刷新后恢复默认示例。
- 首页 `/` 与报价模板 `/quotes/templates` 分别呈现；侧栏入口可导航到报价模板，页面标题为“报价模板 · Dashboard”。
- 1020px 视口改为上下布局，文档宽度未超出视口；桌面 1672px 视口无横向溢出。此前已验证 390px 手机视口。
- 浏览器控制台错误为 0；`npm run check` 通过。

## 模板信息编辑弹窗

- 设计来源：[选定的弹窗视觉稿](docs/references/quote-template-editor-reference.png)；[实现截图](docs/references/quote-template-editor-implementation.png)为 1672 × 941 CSS 像素。
- 点击任一模板行右侧三点直接打开“编辑模板信息”，保留列表和详情作为遮罩下的背景。弹窗在可见视口水平、垂直方向的中心偏差均为 0px。
- 弹窗可修改名称、适用客户和最多 200 字备注。浏览器验证：取消后不保存；修改“保养常用件”并保存后，列表标题、详情标题、客户和备注同步更新；刷新恢复默认示例。
- 视觉稿中的浅色表单、细分隔线与深色保存按钮已还原；具体大小随视口收缩，弹窗内容在窄屏可滚动。

## 边界与后续

模板和报价草稿目前只在页面内存中，刷新会恢复默认示例。真实零件号、适配、库存、价格以及跨设备持久化需要后续数据源。当前 UI 会明确提醒人工核对，复制报价不会自动发送给客户。

# SKU 模块验收（2026-09-22）

final result: passed

- 来源：docs/references/sku-reference.png，1672×941。
- 实现：docs/references/sku-implementation.png，1672×941；CSS viewport 1672×941，1:1 像素，无密度缩放。
- 全图并排证据：docs/references/sku-comparison.png；局部表头证据：docs/references/sku-detail-comparison.png。
- 其他状态：sku-mobile.png（390×844），sku-editor.png（1672×941）。
- 状态：/skus，已登录隔离测试账号，仅一条明确标注的 QA 测试 SKU，客户价 ¥128.50；参考图为多条示意数据，不能据此比较记录数量。正式数据库未写入测试数据。
- 有意调整：依据用户最新结论新增性质、产地及客户价栏；沿用项目真实导航，新增客户与账号入口。未实现批量导入，未展示无效导入按钮。原参考单一售价替换为客户专属价。

## 检查及迭代

1. 初次检查发现页面根容器未计入内边距，导致多余竖向滚动；补充 border-box。
2. 手机检查发现无障碍隐藏文本保留了表格外侧绝对定位，页面 scrollWidth 达到 1129；固定隐藏文本原点后，390px 视口的 scrollWidth 为 390。
3. 修正后重新截图并与来源并排核对：贴边导航、标题/搜索/筛选/表格/页脚层级完整；新增字段导致表格比旧参考更紧凑，没有剩余 P0/P1/P2 问题。
4. 居中编辑弹窗实测中心 (836,470.5)，等于视口中心。长表单在弹窗内部滚动。
5. 浏览器验证客户新建、SKU 新建、客户价格保存及刷新持久化、编辑弹窗；控制台 error 日志为空。权限与重启持久化另有后端集成测试覆盖。

## 剩余边界

批量导入、库存预警、出入库流水和原报价模板与真实 SKU 的联动均未实现。大量真实数据与公网反向代理需上线前另行验收；当前未部署公网。

用户最新范围：仅以电脑端交付和后续验收，不要求手机端专项适配；以上窄屏记录是本轮已完成的基础检查。

# 客户档案分区编辑器验收（2026-09-23）

final result: passed

## 对照证据

- 视觉来源：`docs/references/customer-editor-reference.png`，1487 × 1058 像素，为本轮用户选定的方案 3。
- 浏览器实现：`docs/references/customer-editor-implementation.png`，1265 × 712 CSS 像素，deviceScaleFactor 1；Codex 应用内浏览器本机 `/customers` 编辑态。
- 合并比较：`docs/references/customer-editor-comparison.png`，1280 × 1902 像素；来源和实现分别等比缩放到 1280px 宽并纵向排列，没有拉伸。两者宿主视口纵横比不同，因此重点比较弹窗自身的信息层级、左右比例、控件密度和固定操作区，不将背景页面裁切差异计为缺陷。
- 状态：隔离 `.qa-data` 中的“视觉验收客户”，基本资料分区；正式数据库未写入 QA 数据。
- 全图中文、输入框和导航均清晰可读，不需要额外局部裁切；联系方式、多联系人、开票与地址分区通过浏览器 DOM 与实际操作单独验证。

## 五项视觉检查

| 检查面     | 结果                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 沿用项目中文系统字体，主标题、分区标题、字段标签和辅助文案层级与来源一致；未复刻生成图中的异常字形。        |
| 间距与布局 | 1080px 桌面弹窗、264px 左侧资料导航、双列表单、完整度横条和固定页脚与选定方向一致；较矮视口内正文独立滚动。 |
| 颜色       | 白色正文、浅灰蓝导航底、蓝色选中态与深色主按钮映射现有产品色板，对比度清晰。                                |
| 素材与图标 | 视觉稿无照片或插画；资料分区使用项目已有 Tabler Icons，头像为客户名称首字的标准 UI 占位，不存在缺失素材。   |
| 文案与内容 | 五个分区、自动客户编号、资料完整度、主营品牌字典、多个联系人、开票结算与地址字段均为真实可保存业务字段。    |

## 比较与修正历史

1. 初版已还原左侧分区导航和基本资料，但视觉来源中的“添加联系人”尚未形成真实业务能力（P1）。新增 `customer_contacts` 数据表、联系人新增/删除控件和事务持久化，并在详情侧栏展示其他联系人。
2. 热更新期间旧接口响应没有 `additionalContacts`，详情组件出现一次空值错误（P1）。编辑入口和详情展示增加兼容性默认值，重启后旧数据库迁移补建联系人表；再次检查无错误。
3. 最终比较中，来源为 1487 × 1058 生成图，实现捕获受应用内浏览器可用区域限制为 1265 × 712；已按等宽归一化比较。实现保留同一信息架构并针对矮视口压缩垂直空白，属于响应式处理。没有未解决的 P0 / P1 / P2。

## 交互与运行检查

- 实测五个资料分区切换、主营品牌字典展开、字段填写、资料完整度实时变化、新建客户保存、`KH20260923001` 自动编码以及详情展示。
- 实测添加第二联系人，填写姓名、电话、微信和邮箱后保存；详情出现“采购联系人”，刷新后的 API 数据由后端集成测试覆盖。
- 浏览器 console error / warning 为 0。
- `npm run check` 通过：格式、lint、统一控件检查、18 项后端测试、TypeScript 与 Vite 生产构建全部成功。

# 最终参考图一比一结构验收（2026-09-23）

final result: passed

## 对照证据

- 视觉真值：`E:/Codex/Home/generated_images/01a0cbfc-38dc-7b33-9089-f12a2797dee8/exec-7531c768-be0b-4d7d-b325-9795ebe15f55.png`，1672 × 941 像素。
- 浏览器实现原始截图：`docs/references/site-reference-layout-home-raw.png`，1664 × 941 像素；浏览器 CSS 视口 1672 × 941，deviceScaleFactor 1。应用内浏览器截图接口横向少 8 像素，因此仅将原始截图横向归一化到 1672 × 941，归一化结果保存为 `docs/references/site-reference-layout-home.png`；页面运行时 `scrollWidth × scrollHeight` 实测为 1672 × 941，无横向或纵向溢出。
- 完整并排对照：`docs/references/site-reference-layout-comparison.png`，3344 × 973 像素；参考图与归一化实现均保持 1672 × 941，未裁切页面内容。
- 聚焦对照：`docs/references/site-reference-layout-nav-comparison.png` 检查顶部导航、搜索、标题和主操作；`docs/references/site-reference-layout-panels-comparison.png` 检查左右面板、阶段条、表头、圆点和状态标签。
- 状态：本机隔离 QA 管理员登录态；客户、待办和模板均来自真实测试数据库。参考图中的示意客户、任务、数量和模板没有写入数据库。

## 五项视觉检查

| 检查面     | 结果                                                                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 应用名称、主导航、38px 页面标题、23px 面板标题、15–16px 业务正文和 12–14px 辅助文字与参考图形成相同层级；沿用系统中文字体以避免额外字体加载和字形偏差。              |
| 间距与布局 | 参考视口下导航总高 113px；内容左右边距 43px；主面板顶边 258px；左面板约 1038px、间距 15px、右面板约 533px；底部面板从 661px 开始。运行时无滚动溢出。                 |
| 颜色       | 页面底、近白表面、亮蓝主操作、蓝黑正文、蓝灰辅助文字、冷灰边框，以及阶段圆点、优先级和状态色均与参考图的冷暖关系及明度层级一致。                                     |
| 素材与图标 | 参考图无照片、插画或品牌图像；实现使用已有 Tabler Icons 对应搜索、帮助、通知、箭头、加号与更多操作，没有用文本符号、手绘 SVG 或占位图替代可见素材。                  |
| 文案与内容 | 标题、面板名、阶段名、表头和操作文案按参考图还原；业务行仍读取真实客户与模板，因此记录数量、客户名、时间和空状态会随数据库变化，这是数据真实性约束，不属于布局偏差。 |

## 比较与修正历史

1. 上一版保留“业务工作台”标题、三块业务概览和“待补充资料”面板，与参考图结构不一致（P1）。本轮删除额外概览，严格替换为“客户与报价”、四阶段项目进展、“今日优先处理”和最近模板。
2. 初次实现的导航同时高亮“工作台”和“客户与报价”，搜索框横坐标偏左，标题被全局 29px 规则覆盖，页面还出现 2–4px 垂直溢出（P1/P2）。主导航改用明确模块状态；搜索框校准到 x=737；标题提高选择器优先级并恢复 38px；导航和页面高度重新核算到 941px。
3. 第二次对照中左面板比参考图宽约 1px、面板高约 1px，主按钮偏窄（P2）。网格改为 1.9475:1、15px 间距、386px 高度，按钮水平内边距增至 29px；最终主面板坐标与参考图一致，没有未解决的 P0 / P1 / P2。

## 交互与运行检查

- 浏览器实际点击“新建客户”进入 `/customers`；顶部功能搜索输入 `SKU` 并回车进入 `/skus`。
- 1280 × 720 中等桌面视口实测 `scrollWidth` 等于可用内容宽度，没有横向溢出；辅助工具会按断点收起。
- 最终干净浏览器页面 console error / warning 为 0。
- `npm run check` 通过：格式、lint、统一控件检查、18 项后端测试、TypeScript 与 Vite 生产构建全部成功。

## 后续细节

- P3：负责人和结算方式当前为自由文本；如后续在其他模块复用，应再建立共享字典或账号选择器，避免名称漂移。

# 报价模板 SKU 清单改版验收（2026-09-23）

final result: passed

## 对照证据

- 视觉来源：`E:\Codex\Home\generated_images\01a0cbfc-38dc-7b33-9089-f12a2797dee8\exec-9329e12c-a76b-421d-abe2-f7b4920bb675.png`，1668 × 856 像素，为用户选定的第 3 个方向。
- 浏览器实现：`http://127.0.0.1:4279/quotes/templates`，Codex 应用内浏览器，1668 × 856 CSS 视口，deviceScaleFactor 1；已捕获并检查主界面及“选择已有 SKU”抽屉展开态。
- 状态：隔离 QA 数据中选中“卡宴E3”，主表含两个价格条目；抽屉含四条现有 SKU。正式数据库未写入 QA 数据。
- 全图比较覆盖整体比例、模板栏、输入工具条、表格、遮罩、抽屉、搜索框和固定底部操作区；表格与抽屉文字在同尺寸全图中可读，无需额外局部裁切。

## 五项视觉检查

| 检查面     | 结果                                                                                                                |
| ---------- | ------------------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 沿用 Helvetica Neue / 苹方 / 微软雅黑；22px 区域标题、15px 正文和 13px 表头与目标层级一致。                         |
| 间距与布局 | 主界面使用单行 SKU 工具条与轻量表格；抽屉宽 620px、固定右侧并保留底部操作区。项目全局导航作为既有产品结构继续保留。 |
| 颜色       | 白色基底、浅蓝提示条、浅灰表头与分隔线、蓝色主操作符合选定视觉方向；遮罩未影响抽屉正文对比度。                      |
| 素材与图标 | 页面无照片或插画；数据库、搜索、关闭等动作使用项目既有 Tabler Icons，没有自绘图形或占位素材。                       |
| 文案与内容 | SKU 编码、品牌、名称、价格为主字段；“输入 SKU 编码”和“从 SKU 库添加”两种入口均清晰可见。                            |

## 比较与修正历史

1. 初次浏览器检查发现默认 864px 窗口触发响应式布局，不适合作为桌面目标对照；切换到与视觉稿相同的 1668 × 856 视口后重新捕获。
2. 抽屉多选实测发现，旧模板条目没有 `skuId` 时，同编码 SKU 可能被再次加入（P1）。去重规则改为同时比较 `skuId` 与不区分大小写的 `skuCode`，随后重新运行全部检查。
3. 最终桌面状态中模板栏、输入工具条、字段表格和右侧抽屉均与视觉稿保持相同信息层级；保留项目既有全局导航属于产品约束，不是视觉缺陷。无未解决的 P0 / P1 / P2。

## 交互与运行检查

- 浏览器实测打开 SKU 抽屉、搜索结果读取、勾选两条 SKU、按钮计数更新和批量加入；抽屉关闭后表格更新。
- 手输编码精确命中时读取 SKU 快照，未命中时生成可继续补全品牌、名称和价格的自定义条目；后端集成测试覆盖模板字段持久化与重启读取。
- 浏览器控制台 error / warning 为 0。
- `npm run check` 通过：格式、lint、统一控件检查、18 项后端测试、TypeScript 与 Vite 构建全部成功。

## 后续细节

- P3：视觉稿未定义超过 20 条搜索结果的抽屉分页；当前沿用 SKU API 的第一页 20 条，并通过即时搜索缩小结果。

# 客户项目看板验收（2026-09-22）

final result: passed

- 目标图：[用户指定的客户项目视觉稿](docs/references/customer-project-reference.png)，1487 × 1058。实现截图：[隔离测试数据下的页面](docs/references/customer-project-implementation.png)，1488 × 1058。两者同高度、宽度相差 1px；在应用内浏览器按相同桌面视口检查。
- 实现保留视觉稿的左侧导航、顶部客户类型筛选、四色阶段列、客户卡片与右侧详情。正式页面从数据库读取客户；截图中的五位客户只存在于隔离的 QA 数据目录。
- 有意差异：视觉稿中的报价数量、报价历史和部分活动属于示意数据，现有系统没有对应的客户报价实体，因此实现展示真实空状态；卡片中的“询价”只来自现有客户备注。
- 修正了初版 1265px 浏览器宽度下第四列被截断的问题：该路由收窄导航，并调整阶段列最小宽度。复查时四列和详情栏均完整显示。
- 浏览器实际操作通过：客户类型筛选、阶段从“询价中”移至“已报价”、添加跟进记录，以及刷新后的阶段和记录持久化。`npm run check` 全部通过。

## SKU 供货性质调整（2026-09-22）

- 在隔离测试账号的新增 SKU 弹窗中展开统一 Select，确认依旧站父子层级显示 10 个选项：原厂件及三个子项、品牌件及四个子项、待核实；无浏览器原生下拉菜单。
- 正式字典来自已迁入的 `sku_foundation/supply_type`，停用项不用于新建。历史“待确认”在列表标注为“待核实（旧值）”，筛选“待核实”会包含这类旧记录，原数据库值不改写。

# 顶部导航与全站模块重构验收（2026-09-23）

final result: passed

## 对照证据

- 视觉来源：`docs/references/site-restructure-reference-16x9.png`，1672 × 941 像素；用户最终确认的顶部导航、暖白表面和克莱因蓝方向。
- 首页实现：`docs/references/site-restructure-home.png`，1265 × 712 像素；浏览器 CSS 视口 1280 × 720，deviceScaleFactor 1，截图扣除浏览器垂直滚动区域后保持原始密度。
- 合并比较：`docs/references/site-restructure-comparison.png`，1280 × 1524 像素；来源和实现均归一化为 1280 × 720 后纵向排列，没有非等比拉伸。
- 模块证据：`site-restructure-customers.png`、`site-restructure-quotes.png`、`site-restructure-skus.png`、`site-restructure-dictionaries.png`、`site-restructure-accounts.png`，均来自同一浏览器、同一桌面视口和同一隔离 QA 登录态。
- 状态：本机 `.qa-data` 的真实隔离测试数据；首页读取 1 个启用客户、4 个启用 SKU 和 1 个报价模板。正式数据库未写入视觉验收数据。

## 五项视觉检查

| 检查面     | 结果                                                                                                                                 |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| 字体与层级 | 视觉稿和实现都使用深色高权重标题、14–16px 中文界面正文及较弱的辅助文字；实现沿用系统中文字体，避免额外字体下载和异常字形。           |
| 间距与布局 | 实现还原顶部主导航、模块二级导航、宽内容区、约 2:1 的主要/辅助面板和底部列表；不同模块按业务保留看板、双栏、分组或表格结构。         |
| 颜色       | `#0033a0` 只用于主操作、链接和选中态；`#f5f3ee` 页面底、`#fffdfa` 表面、`#1e2430` 正文和 `#e2ded7` 分隔线与来源一致。                |
| 素材与图标 | 来源无照片或插画；实现使用已有 Tabler Icons，没有用文本符号、CSS 图形或占位图片替代可见素材。                                        |
| 文案与内容 | 视觉稿中的示意客户和模板没有写入系统；实现以真实接口数量、真实最近客户、待补充字段和模板记录替换，同时保留相同的信息层级和操作入口。 |

## 比较与修正历史

1. 上一版全宽深灰侧栏、蓝色大块选中和灰色空首页与最终视觉稿冲突（P1）；本轮替换为顶部主导航与模块二级导航，首页接入真实客户、SKU 和模板数据，并移除大面积深灰表面。
2. 代码初稿仍沿用旧业务页较重的彩色面板和深色客户编辑导航（P2）；通过全局令牌覆盖统一为暖白表面、细边框、浅蓝选中和克莱因蓝主操作，客户编辑器改为浅色资料导航。
3. 浏览器逐页复查首页、客户、报价、SKU、配置字典和账号管理；模块结构不同但颜色、标题、控件、边框、密度和交互状态一致。没有未解决的 P0 / P1 / P2。

## 交互与运行检查

- 顶部四个主模块和二级路由均按权限显示；浏览器实测全局功能搜索输入 `SKU`、选择结果并导航到 `/skus`。
- 客户看板、报价双栏、SKU 筛选表格、字典分组表格和账号列表均完成渲染检查；页面没有横向溢出遮挡持久操作。
- 浏览器 console error / warning 为 0。
- `npm run check` 通过：格式、lint、统一控件检查、18 项后端测试、TypeScript 与 Vite 生产构建全部成功。

## 后续细节

- P3：视觉稿使用更多示意记录，因此 16:9 画面信息更饱满；隔离数据较少时实现保留自然空白和真实空状态，不用虚构数据填充。

# 16:9 参考图配色校准验收（2026-09-23）

final result: passed

## 对照证据

- 视觉来源：`E:/Codex/Home/generated_images/01a0cbfc-38dc-7b33-9089-f12a2797dee8/exec-7531c768-be0b-4d7d-b325-9795ebe15f55.png`，1672 × 941 像素；本轮只提取配色与表面层级，不把其中示意客户数据写入系统。
- 浏览器实现：`docs/references/site-color-calibration-home.png`，1600 × 900 CSS 视口；本机隔离 QA 数据、管理员登录态。
- 并排比较：`docs/references/site-color-calibration-comparison.png`，1600 × 900 像素；上方以同一 16:9 尺寸并排展示参考图和实现，下方记录最终六项全局色彩令牌。

## 五项视觉检查

| 检查面     | 结果                                                                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 保留已确认的页面结构与系统中文字体；正文由偏灰黑改为参考图蓝黑 `#0d1630`，辅助文字统一为蓝灰 `#6a7993`。              |
| 间距与布局 | 本轮没有改变顶部导航、二级导航、首页模块、表格或业务工作区的尺寸和响应式规则，16:9 视口无横向溢出。                   |
| 颜色       | 页面底 `#f5f3f0`、表面 `#fefdfc`、主蓝 `#0047c3`、正文 `#0d1630`、辅助 `#6a7993`、边框 `#dce1e7` 按参考图取样后统一。 |
| 素材与图标 | 沿用已有 Tabler Icons；本次没有新增照片、插画、品牌素材或占位资源。                                                   |
| 文案与内容 | 首页继续读取真实接口数据；参考图的客户、事项和模板仅用于视觉对照，未进入数据库。                                      |

## 比较与修正历史

1. 用户指出上一版与参考图配色差距明显（P1）。实测上一版导航与卡片使用 `#fffdfa` / `#f8f7f3`，边框使用 `#e2ded7`，综合色相偏黄褐；参考图的主要表面更接近冷调近白。
2. 将导航、卡片、输入和弹层归一到 `#fefdfc`，页面底保留轻暖 `#f5f3f0`；主蓝由偏深的 `#0033a0` 调整为参考图高频亮蓝 `#0047c3`，边框与辅助文字改为冷蓝灰。
3. 在 1600 × 900 浏览器视口重新截图并与参考图并排检查；层级、冷暖关系和蓝色明度已一致，没有未解决的 P0 / P1 / P2。

## 交互与运行检查

- 浏览器验证页面加载、顶部导航、真实数据面板和表格均正常，console error / warning 为 0。
- 全局色彩令牌在运行页实际计算值与文档一致。
- `npm run check` 通过：格式、lint、统一控件检查、18 项后端测试、TypeScript 与 Vite 生产构建全部成功。

# 全站配色改版验收（2026-09-23）

final result: passed

## 对照证据

- 色板来源：`docs/references/site-theme-reference.png`，1487 × 1058 像素；用户选定的方案 3 客户编辑器，以克莱因蓝 `#0033a0`、浅灰 `#e6e4df`、深灰 `#4a4a4a` 为主色。
- 浏览器实现：`docs/references/site-theme-customer-editor.png`，1440 × 1024 CSS 视口；辅助页面：`docs/references/site-theme-dictionaries.png`，1440 × 1024 CSS 视口。
- 合并比较：`docs/references/site-theme-comparison.png`，1280 × 1925 像素；来源和实现等比缩放到 1280px 宽后纵向排列，没有拉伸。
- 状态：本机隔离 `.qa-data`、管理员登录态；客户新增弹窗为基本资料分区，字典页为客户类型列表。正式数据库未写入视觉验收数据。

## 五项视觉检查

| 检查面     | 结果                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------- |
| 字体与层级 | 保留既有中文字体与标题体系；深灰正文、浅灰辅助文字和蓝色重点层级清晰，未因主题覆盖改变业务信息结构。                 |
| 间距与布局 | 主题仅统一颜色、边框、阴影和状态，不改变导航宽度、客户编辑器分区、报价工作区、表格或抽屉的既有响应式布局。           |
| 颜色       | 全站统一为深灰导航、暖浅灰页面、白/米白表面和克莱因蓝主操作；hover、focus、selected、disabled 由同一组色板变量派生。 |
| 素材与图标 | 沿用项目已有 Tabler Icons；本次没有新增照片、插画或品牌素材，不存在缺失资源。                                        |
| 文案与内容 | 客户、SKU、字典、报价、账号、登录和空状态的业务文案保持不变；配色改版没有以示意数据替换真实内容。                    |

## 比较与修正历史

1. 初版全局覆盖后，嵌套导航的当前父分组和当前子路由同时显示蓝色（P2），视觉层级过重；父分组改为略亮深灰，仅具体子路由保留蓝色。
2. 深色侧栏使底部账号次级文字和退出按钮对比度不足（P2）；补充半透明边框、浅灰次级文字及蓝色退出按钮状态。
3. 复查客户编辑器、报价模板、SKU 档案和配置中心字典：页面底色、表面、控件、表格、弹窗与抽屉使用同一色板，没有未解决的 P0 / P1 / P2。

## 交互与运行检查

- 浏览器实测客户新增弹窗和配置中心字典页；报价模板与 SKU 档案完成视觉巡检，业务布局和控件状态保持可用。
- 当前浏览器 console error / warning 为 0。
- `npm run check` 通过：格式、lint、统一控件检查、18 项后端测试、TypeScript 与 Vite 生产构建全部成功。

# 保时捷车型库验收（2026-09-23）

final result: passed

## 对照与状态

- 视觉来源：用户选定的“品牌档案馆”方案，以及筛选展开、筛选结果、Macan 95B.3 详情三张 16:9 深化图。
- 浏览器实现：本机隔离验收数据库，Codex 应用内浏览器 1265 × 709（16:9）桌面视口。
- 已验证默认档案馆、代际多选下拉、95B.3 单条结果、Macan 95B.3 详情与代际切换入口。
- 车型家族、代际、动力、年份、状态和识别信息来自 `/api/vehicle-models`；图片为项目内独立车型素材，不使用整张视觉稿作背景。

## 检查与修正

1. 首轮运行发现时代数据中的 `Panamera` 与家族 id 大小写不一致，导致默认页运行时崩溃（P1）；统一为 `panamera` 后首屏正常加载。
2. 详情路由初次被顶部导航识别为工作台（P1）；车型库导航改为包含子路由，详情页继续显示“商品资料 / 车型库”导航状态。
3. 1265px 桌面视口下六列家族卡片过密、筛选结果出现横向滚动（P2）；1500px 以下家族卡片改为三列，清空筛选动作独占末行，复查无内容遮挡。
4. 大幅车型图首次截图时仍在解码，二次捕获确认图片完整显示；筛选展开、结果与详情均与选定视觉方向保持相同的冷暖配色、蓝色选中态、细边框和紧凑信息密度。

## 交互与运行检查

- 实测搜索代际代码、勾选 95B.3、应用筛选、打开详情、返回结果及代际链接。
- 页面使用共享 Select；代际多选支持搜索、勾选、重置、点击外部关闭，无原生下拉外观。
- API 需要登录，读取车型档案的集成测试已覆盖。
- 最终浏览器运行未再出现错误；完整项目检查通过。

## 1920 × 1080 布局复查

- 用户提供的 1920 × 1080 实机截图显示两个 P1 问题：搜索框受共享 `label` 样式影响，输入文字被推到第二行；车型家族卡片的名称、代数和图片共享行内排版，导致文字覆盖车辆。
- 搜索控件增加业务容器内的明确 flex、间距和输入框复位规则；车型卡片改为独立标题行，名称与代数两端对齐，车型图和底盘代码各占独立行。
- 在同一大屏布局断点重新捕获默认页：搜索图标和占位文案水平居中，六张家族卡片无重叠、截断或横向溢出。`final result: passed` 保持有效。

## 历史时代导航交互

- 四个时代卡片已由静态容器改为可聚焦按钮，使用 `aria-pressed` 暴露当前选择状态。
- 浏览器实测“经典时代”“前置发动机”“现代跑车”切换：蓝色选中背景随点击移动，左侧年份、标题与代表车型摘要同步更新，车型家族和代际入口保持独立。
- 修正共享按钮样式造成的时代标题竖排问题；1265px 与大屏六列布局下均保持原有图片比例、时间轴与文本对齐。`final result: passed`。

## 车型家族两级导航

- 家族卡片不再触发筛选或路由跳转；浏览器实测点击 Cayenne 后 URL 保持 `/vehicles`，选中边框切换到 Cayenne，下方同步显示 Cayenne 图片与 955/957、958、E3 三个代际。
- 下方展示所选家族的全部代际，不再截掉四代以上家族的早期车型；代际区可换行，避免 911 的八个代际溢出。
- 只有点击具体代际才进入详情；实测点击 958 进入 `/vehicles/cayenne/958`。包含斜杠的代际代码会进行 URL 编码，避免 955/957 被误拆成两段路由。`final result: passed`。

## 历史车型与透明素材补充

- 车型卡片、年代卡片、筛选结果和生命周期使用透明 PNG，选中态蓝色背景下不再出现白色矩形底。
- 补齐 356、914、924、944、968、918 Spyder 家族和生产年份；年代数据显式保存 `familyIds` 与 `generationCodes` 映射。
- 点击年代仅在当前页联动下方家族及对应代际，提供“显示全部车型”恢复完整档案，不提前进入详情路由。

## 车型详情宽屏紧凑布局验收（2026-09-26）

- Source visual truth: `C:/Users/ADMINI~1/AppData/Local/Temp/codex-clipboard-21ae9d9e-3076-4627-827d-56851707f44b.png`（1920 × 921，Macan 95B.3 详情滚动状态）。
- Implementation: `http://127.0.0.1:4179/vehicles/macan/95B.3`，Codex in-app browser 1920 × 921 CSS viewport、1× density；浏览器实测截图保存在本次 CUA 验收记录中。
- State: 已登录临时 QA 管理员，Macan 95B.3 选中；并切换至 95B.2 验证路由和生命周期选中态。

### Full-view comparison

- 原截图在 1920 屏幕可用高度约 921px 时，主体仍占据接近全宽，主图 385px，必须滚动后才能同时看到生命周期条和下方内容。
- 紧凑布局将内容上限收至 1680px、左右居中，主图容器降至 310px，生命周期条降至 78px；下方“快速识别/车型版本”在页面顶部状态即可进入首屏。
- 实测主体宽度 1680px，主图容器 310px，生命周期条 78px，下方内容起点约 694px；页面总高从原布局约 1182px 降至约 1058px。

### Focused comparison

- 字体与文案：标题缩至 30px，其余原有字号、字重和中文内容保持一致，无新增换行或截断。
- 间距与节奏：详情信息行、说明提示、代际卡片和表格行同步收紧，仍保留可辨识的分组边界与点击面积。
- 色彩：沿用既有暖灰背景、蓝色选中态和边框 token，无颜色漂移。
- 图片：保持原始 Macan 图片和 `object-fit` 规则，仅改变容器高度；未发现拉伸、透明边缘或清晰度问题。
- 交互：点击 95B.2 后 URL、H1 和高亮代际均正确切换；浏览器控制台无 warning/error。

### Comparison history

- P2：1920 宽屏低视口下主体过宽、主图和生命周期条过高，首屏信息密度偏低。
- Fix：增加仅在 `min-width: 1600px` 且 `max-height: 1000px` 生效的紧凑布局，收窄最大宽度并降低垂直尺寸，不影响普通桌面和移动断点。
- Post-fix evidence：1920 × 921 实测尺寸符合上述数值，页面无水平溢出，详情和代际切换正常。

### Findings

- 无残留 P0/P1/P2 问题。

### Follow-up polish

- P3：若后续希望四条车型版本在 1080 屏幕全部可见，可再把快速识别图片从 96px 微调至 88px，但当前不影响核心浏览。

final result: passed
