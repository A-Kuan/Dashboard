# 仓库协作规则

适用于整个仓库内的人类开发者和 AI 编码助手。开始工作前阅读本文件、README.md 和本次改动涉及的 docs 文档。

## 全局文档同步规则（用户明确要求）

**每次改动涉及全局约定或已有全局文档描述的内容时，必须及时更新对应文档，并与实现放入同一次提交。不能把文档更新留到下一次任务。**

全局文档包括本文件、README.md、CONTRIBUTING.md、CHANGELOG.md 和 docs/ 内的文档。对应关系：

| 改动内容                                             | 同步更新                                              |
| ---------------------------------------------------- | ----------------------------------------------------- |
| 项目用途、已实现能力、启动入口                       | README.md                                             |
| 技术栈、模块职责、目录、路由、共享组件、数据来源     | docs/ARCHITECTURE.md                                  |
| 环境版本、依赖管理、命令、环境变量、端口、多环境协作 | docs/DEVELOPMENT.md，必要时 README.md 与 .env.example |
| 部署方式、托管配置、构建产物                         | docs/DEPLOYMENT.md                                    |
| 导航样式、菜单配置、交互与参考来源                   | docs/NAVIGATION.md，必要时 docs/ARCHITECTURE.md       |
| 协作流程、提交和检查要求                             | CONTRIBUTING.md 与本文件                              |
| 用户可见功能、基础设施或全局规则变化                 | CHANGELOG.md                                          |

交付时简要说明更新了哪些文档；若本次修改不影响文档，说明“不涉及全局文档变更”。不要为凑更新修改无关内容。PR 检查项必须如实填写；目前这是协作约定，CI 不会自动判断文档语义是否准确。

## 工程约定

- 使用 React + TypeScript + Vite；Node.js 与 npm 以 .nvmrc、package.json 为准。
- 使用 npm，提交 package-lock.json；换环境执行 npm ci，不混用锁文件。
- TypeScript 使用严格模式。共用页面结构和组件放在 src/components；业务逻辑按功能放在 src/features。
- 当前范围含基础骨架、左侧导航、独立报价模板，以及用户授权的客户、SKU、字典与身份验证模块。首页保持最小骨架；导航按 docs/NAVIGATION.md 与参考图实现，报价模块按用户选定的视觉图实现。新增业务页面或真实数据接入需有明确需求，不擅自添加主题或整套 UI 库。
- 重复出现的选择项使用共享字典，不在各页面各写一份。
- 业务页面不得暴露浏览器或操作系统的原生控件外观。下拉、复选框、数字和日期输入等交互须使用项目统一的可见样式；下拉菜单使用共享 Select 组件，不直接渲染原生 `<select>`。保留语义、键盘操作、焦点提示与表单提交能力，开发后检查展开态和弹窗内布局。`npm run ui:check` 会拦截原生下拉及日期弹窗。
- 已授权新增登录、客户档案、SKU、报价模板和客户专属价格模块，使用 Node.js + SQLite 持久保存。报价草稿仅用于本次复制文字，不保存为模板。清晰标记空状态，不能用虚构指标冒充实际数据。
- 权限必须由服务端校验；前端隐藏入口不能充当安全控制。
- 不提交 node_modules、dist、日志、真实 .env、账号密钥或用户业务数据。
- 完成改动后执行 npm run check。新增业务行为时补充与风险相称的验证，简单样式改动不堆砌测试。
- 开始工作先检查 git status；保留用户未提交修改。提交前检查实际差异，禁止强推覆盖其他环境的工作。
- 本规则文件不是运行状态记录；进展写 CHANGELOG.md，稳定约定写对应 docs 文档。
