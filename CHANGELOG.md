# 变更记录

## 未发布

- 严格按用户最终 1672 × 941 参考图重做首页与顶部导航：去除额外业务概览条，首页固定为“客户与报价”标题、客户项目进展、今日优先处理和最近报价模板四个区域；还原阶段圆点、表格密度、帮助与通知入口、搜索快捷键及精确面板比例，同时继续读取真实客户和模板数据。

- 按最终 16:9 参考图重新校准全站配色：页面底保留轻暖骨白，导航和卡片改为冷调近白，主蓝由偏深的 `#0033a0` 调整为参考图亮蓝 `#0047c3`，正文、辅助文字和边框同步改为蓝灰体系，消除上一版明显的黄褐感。

- 按选定的 16:9 克莱因蓝视觉稿重构全站：左侧导航改为顶部主导航与模块二级导航，新增可用的功能入口搜索和账号菜单；首页改为读取真实客户、SKU 与报价模板数据的业务工作台，并统一客户、报价、SKU、字典、账号和登录模块的暖白表面、细边框、表格密度与交互状态。

- 全站改用用户选定的克莱因蓝、浅灰和深灰配色：统一导航、页面背景、表单、表格、弹窗、抽屉、登录页和主要操作状态，并修正嵌套导航父子两级同时高亮的问题。

- 客户新增与编辑按选定的侧边导航方案重做为五个资料分区，新增客户编号自动生成、资料完整度、客户来源、负责人、微信、邮箱、主营品牌、标签、开票结算、地址及多联系人持久化；旧客户无损补齐空字段。

- 修复报价模板价格输入在每次按键后立即格式化、导致无法连续输入 `1200` 等金额的问题；编辑时保留原始输入，失焦后再校验并转换为金额。

- 加宽报价模板的 SKU 选择抽屉，并提高搜索框高度、放宽编码与价格列，改善大屏下过窄和信息拥挤的问题。

- 优化报价模板 SKU 选择抽屉的搜索框焦点态，移除内外双重边框，改为统一圆角边框和柔和焦点光晕。

- 修复报价模板品牌选择器未加载共享样式、展开后显示浏览器默认按钮外观的问题；共享 Select 现在自带统一样式，脱离业务页面容器也保持一致表现。

- 报价模板品牌改用商品品牌字典选择；添加已有 SKU 或手输编码精确命中时自动带出名称、品牌和售价，同行价缺失时回退修理厂价。

- 客户类型与客户跟进阶段接入配置中心字典；恢复系统设置中的维护入口，客户表单、筛选、看板分列和服务端校验统一读取启用字典项。

- 清理当前功能未使用的字典：删除配置中心全部 18 组，以及商品基础中的“货品状态”“资料来源类型”，共删除 20 组；保留 SKU 实际使用的 8 组。同步移除已清空的配置中心字典导航和路由，数据删除前已分别备份线上与本地数据库。

- 报价模板改为 SKU 清单形式，可直接输入 SKU 编码或从右侧 SKU 库搜索、多选添加；条目保存 SKU 引用、编码和价格快照，品牌、名称与价格支持表格内调整。

- 按用户确认将现有 12,248 条 SKU 的品牌统一设为“保时捷原厂”，供货性质统一设为 `porsche_genuine_original_pack`；线上与本地均先创建完整数据库备份，再用事务更新并通过完整性和数量校验。

- 所有字典分组新增选项时编码改由服务端自动生成，界面不再要求人工填写；已有迁移编码不改写，避免影响历史关联。

- 统一业务 Select 增加选项搜索、清空、空结果提示和搜索后的键盘导航；全部现有业务下拉自动获得搜索能力。

- SKU 表单和筛选进一步接入商品基础字典：商品品牌、安装位置、适配汽车品牌、经营状态和适配核实状态改用统一选择控件；安装位置支持组合选择，旧值继续可编辑。服务端同步校验启用字典项。

- 修正反向代理后的登录限流：仅信任本机 Nginx 提供的合法来源 IP，避免所有公网用户共用同一限额。

- 新项目独立部署到服务器 HTTPS 443 端口，旧站继续使用 HTTP 80；新增 systemd、Nginx 和短期 IP 证书自动续期配置。本地 SQLite 经一致性快照迁入服务器，代码与业务数据分离。

- 字典列表按父子层级排列：一级项在前，二级项缩进显示；搜索只命中子项时显示所属上级，修复“供货性质”子项先于父项出现的问题。

- SKU“供货性质”改用旧站商品基础字典的编码与父子层级，列表和筛选显示中文名称；新建 SKU 默认“待核实”。保留已有 SKU 的历史值，不批量修改已导入资料。

- 客户档案入口改为“客户项目”：原 `/customers` 路由展示按跟进阶段分列的看板，支持客户类型筛选、搜索和详情。
- 跟进阶段及动态记录持久化到 SQLite；旧客户默认进入“待跟进”，报价记录未关联时显示空状态。

- 报价模板改为 SQLite 持久化，支持新建、修改、删除及模板内配件维护；删除前显示确认弹窗，列表刷新后从数据库恢复。读写按现有登录角色校验，空库不再自动显示示例模板。

- 左侧导航按工作台、客户与报价、商品资料、系统设置重新分组，SKU 入口改称“SKU 档案”；进入子页面时自动展开所属分组，账号入口继续只对管理员显示。

- 迁入旧站配置中心 18 组、3,149 个字典项，以及商品基础 10 组、87 个字典项和 5 个品牌 Logo；提供独立页面、分页搜索、管理员维护及来源保留。SKU 编辑器接入商品品类和基本单位，其他不同语义的字典保持独立。

- 业务下拉改用统一的自绘菜单，修正 SKU 分类选项弹出时的系统原生样式；同时统一复选框、数字与日期输入外观，并将禁止原生控件外观写入开发规则。

- 删除名称含“弃用零件”的 246 条 SKU 及其关联件号、车型；删除前备份数据库。旧站导入脚本同步排除这些记录，防止再次导入，当前 SKU 共 12,248 条。

- 从旧站只读导出并导入 12,494 条启用 SKU、12,463 条关联件号及 12,464 条车型适配；保留原始来源记录，缺失的品牌、性质、产地与售价不推断。SKU 列表改为服务端分页和搜索，迁移前自动备份数据库。

- 客户新增同行、修理厂、待分类类型；SKU 新增两档售价，原客户专属价作为协议价优先使用。旧数据迁移保留原价，旧客户先标为待分类。
- 新增逐条供应商报价历史与最近报价参考；快速报价按客户取价，支持一次性调价、展示与供应商报价差额并复制客户报价文字，不将差额称为利润。

- 取消管理员初始化和新增账号的密码 12 位最低长度限制，保留必填及 128 位上限。

- 新增本地 Node.js + SQLite 后端、登录与角色权限，支持一次性初始化管理员、账号启停、会话注销和业务写入审计。
- 新增客户档案和汽配 SKU 模块，支持配件性质、产地、关联件号、车型适配、库存库位、客户专属价格、启停及筛选；客户和 SKU 数据持久保存。
- 后端集成测试纳入 npm run check，开发命令同时启动前后端；更新环境、部署与数据备份文档。尚未部署公网，原报价模板仍为内存演示。

- 模板行右侧三点改为直接打开居中的信息编辑弹窗，可修改名称、适用客户与备注；保存后同步更新列表和详情。
- 移除模板栏顶部重复的“报价模板”标题，搜索与新建入口直接置于栏顶。
- 按用户最终选定的视觉图重做报价模板列表：437px 贴边直角栏、分组模板、搜索与深色新建按钮、底部管理入口；管理弹窗可删除模板，窄屏改为上下布局。
- 将报价模板从首页移至独立的 `/quotes/templates` 模块，并按导航参考图设置顶层入口；首页恢复最小骨架。
- 按用户选定的 16:9 视觉图新增报价模板模块，支持搜索、创建、编辑车型配件组合，以及填写并复制报价草稿；当前数据仅存于页面内存。
- 将侧栏宽度调整为 256px，并加入报价模板工作区的三栏布局。
- 移除布局四周留白，使侧栏贴齐视口边缘并占满桌面视口高度。
- 按用户提供的参考图加入白色侧栏、灰色图标、浅灰选中态和弧形层级连接线；后续按要求取消侧栏外侧留白与圆角。
- 新增配置驱动的导航组件，支持分组折叠、嵌套链接、可选计数和手机端菜单。
- 初始只接入首页，后续将首页扩展为报价模板工作区。
- 引入 Tabler Icons，更新架构、README、协作规则与导航文档，保存视觉参考。

## 0.1.0 — 2026-09-22

- 初始化 React + TypeScript + Vite 基础骨架，启用严格类型检查。
- 加入基础路由、共享布局入口、最小首页与 404。
- 按用户要求不包含自定义样式、主题、UI 库、演示数据或业务页面。
- 统一 Node/npm 环境、精确依赖、锁文件、格式和 lint 检查。
- 配置 GitHub Actions 的 Linux / Windows 检查与 PR 模板。
- 建立架构、开发、部署、协作文档，以及全局文档随相关修改同次更新的规则。
- 当前没有后端、登录、权限、数据库或公网部署。
